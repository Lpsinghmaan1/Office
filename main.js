let text = "<h1>Winter is coming</h1>";
let myRegex = /<.*?>/; // Change this line
let result = text.match(myRegex);

console.log(result)

class People {
constructor (name,area,age)
 {
     this.name = name;
     this.area = area;
     this.age = age;
}
}

const lp = new.People("lp","support",28)

const myobject = {
    name : 'lp',
    Age : '28',
    human : true
}

myobject.origin = 'india';

console.log(myobject);